<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>gr.uoa.di.madgik.federation</groupId>
		<artifactId>search-aggregator</artifactId>
		<version>${revision}</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<artifactId>search-aggregator-service</artifactId>
	<packaging>jar</packaging>
	<name>Federation Search Aggregator Service</name>
	<description>Spring Boot application that aggregates search results from multiple Resource Catalogue nodes into a single federation API endpoint.</description>

	<properties>
		<maven.deploy.skip>true</maven.deploy.skip>
		<tomcat.version>11.0.26</tomcat.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>gr.uoa.di.madgik.federation</groupId>
			<artifactId>search-aggregator-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.wiremock</groupId>
			<artifactId>wiremock-standalone</artifactId>
			<version>3.9.1</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>3.0.3</version>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webflux</artifactId>
		</dependency>

		<dependency>
			<groupId>gr.uoa.di.madgik</groupId>
			<artifactId>registry-core-api</artifactId>
		</dependency>
		<dependency>
			<groupId>gr.uoa.di.madgik</groupId>
			<artifactId>registry-core-rest</artifactId>
		</dependency>
		<dependency>
			<groupId>gr.uoa.di.madgik</groupId>
			<artifactId>eosc-node-registry-client</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<skip>false</skip>
					<image>
						<name>${spring-boot.build-image.imageName}</name>
						<cleanCache>true</cleanCache>
						<buildpacks>
							<buildpack>docker://docker.io/paketobuildpacks/java</buildpack>
							<buildpack>docker://docker.io/paketobuildpacks/health-checker:latest</buildpack>
						</buildpacks>
						<env>
							<BP_HEALTH_CHECKER_ENABLED>true</BP_HEALTH_CHECKER_ENABLED>
						</env>
					</image>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
